{
  "name": "nichhome-zabbix-console",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first NichHome Uptime Systems-style console for Zabbix.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check server/zabbix.js && node --check server/grafana.js && node --check server/config.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
